# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Test artifacts
tests/output/
tests/temp/

# Flow Framework — runtime + machine-local + per-user workspace
.flow/.runtime/
.flow/.current-task
.flow/config.local.yaml
.flow/workspace/*
!.flow/workspace/.gitkeep
.flow/**/*.tmp
.flow/**/.backup-*
.flow/tasks/**/.checkpoint/
# v0.8.5 — per-task dispatch telemetry (raw events, observability-only)
.flow/tasks/**/telemetry.jsonl

# Claude Agent SDK local scratch (ephemeral worktrees, etc.)
.claude/

# Worktrees (subagent-driven feature isolation)
.worktrees/
.context/
